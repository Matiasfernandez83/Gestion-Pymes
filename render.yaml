services:
  - type: web
    name: gestion-pymes
    env: docker
    plan: free 
    healthCheckPath: /login
    envVars:
      - key: DB_PATH
        value: /app/database.db
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: '5000'
